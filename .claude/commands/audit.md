your goal is to update any vulnerable dependencies.

Do the following:
1. Run `npm audit` to see what vulnerabilities you have.
2. Run `npm audit fix` to fix the vulnerabilities.
3. Run tests and verify that update didn't break anything.